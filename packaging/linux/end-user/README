		╭─────────────────────────────────╮
		│ END-USER BUILD SCRIPT FOR GEM++ │
		╰─────────────────────────────────╯

This version is intended to be distributed on a system where
the user have root privileges. The script "build.sh" compiles
the software, makes the documentation be up-to-date, and
builds a .deb package that can be installed and uninstalled
using dpkg.

Here is the obtained installation tree :
/usr/local/
		├── bin/
		│	├── GEM++dist
		│	├── GEM++gui
		│	├── GEM++multidist
		│	├── GEM++multisub
		│	└── GEM++sub
		└─ lib/
			├── libGEM++Cplex.so # GEM++ libs
			├── libGEM++GLPK.so
			├── libGEM++Gurobi.so
			├── libGEM++.so
			├── libicudata.so.51 # Qt libs
			├── libicui18n.so.51
			├── libicuuc.so.51
			├── libQt5Core.so.5
			├── libQt5Gui.so.5
			├── libQt5Widgets.so.5
			└── libQt5XML.so.5
